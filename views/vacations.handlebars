<h1>Vacation</h1>
{{#each vacations}}
	<div class="vacation">
		<h3>{{name}}</h3>
		<p>{{discription}}</p>
		{{#if inSeason}}
			<span class="price">{{price}}</span>
			<a href="/cart/add?sku={{sku}}" class="btn btn-default">Buy Now!</a>
		{{else}}
			<span class="outOfSeason">We'er sorry, this vacation is currently not in season.</span>
			{{! The "notify me when this vacation is in season" page will be our next task. }}
			<a href="/notify-me-whe-in-season?sku={{sku}}">Notify me when this vacation is in season.</a>
		{{/if}}
	</div>
{{/each}}
